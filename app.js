//Crea una web que pida, por medio de un prompt(), una frase al usuario
//devuelver el mismo mensaje encriptado según el algoritmo de Cifrado César con el parámetro de desplazamiento de 33 espacios hacia la derecha

var text = prompt("Introduce text");

  var alphabethArray = [];
  for (var i = 0, < 26, i++){
    var letter = string.fromCharCode(i + asciiCode);
  }
}
